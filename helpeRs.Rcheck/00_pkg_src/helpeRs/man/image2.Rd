% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpeRs_Visualizers.R
\name{image2}
\alias{image2}
\title{Plot a matrix as an image with intuitive orientation}
\usage{
image2(
  mat,
  xaxt = NULL,
  yaxt = NULL,
  col = NULL,
  main = NULL,
  scale_vec = c(1, 1.04),
  cex.axis = 1
)
}
\arguments{
\item{mat}{Numeric matrix to display as a heat map image.}

\item{xaxt}{Optional character vector of labels for the x-axis (columns).
If \code{NULL} (default), no x-axis labels are drawn.}

\item{yaxt}{Optional character vector of labels for the y-axis (rows).
If \code{NULL} (default), no y-axis labels are drawn.}

\item{col}{Optional vector of colors for the heat map. If \code{NULL},
uses the default \code{image()} color palette.}

\item{main}{Optional character string for the plot title.}

\item{scale_vec}{Numeric vector of length 2 controlling the placement scaling
of tick labels on the x and y axes. Default is \code{c(1, 1.04)}.}

\item{cex.axis}{Numeric value for character expansion of axis tick labels.
Default is 1.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effect of producing
  a plot.
}
\description{
A wrapper around \code{\link[graphics]{image}} that displays matrices with
the conventional orientation (first row at top, first column at left). The
base \code{image()} function displays matrices transposed and flipped, which
can be confusing. This function corrects that behavior.
}
\examples{
# Create a simple matrix
mat <- matrix(1:12, nrow = 3, ncol = 4)
rownames(mat) <- c("A", "B", "C")
colnames(mat) <- c("W", "X", "Y", "Z")

# Plot with row/column labels
image2(mat, xaxt = colnames(mat), yaxt = rownames(mat), main = "Example")

}
\seealso{
\code{\link{heatmap2}} for a higher-level heat map function,
  \code{\link{heatMap}} for interpolated heat maps from scattered data
}
