% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpeRs_Visualizers.R
\name{heatmap2}
\alias{heatmap2}
\title{Quick heat map visualization of a matrix}
\usage{
heatmap2(
  mat,
  row_labels = NULL,
  col_labels = NULL,
  use_gg = FALSE,
  scale = FALSE,
  log = FALSE,
  includeMarginals = FALSE,
  ...
)
}
\arguments{
\item{mat}{Numeric matrix or data frame to visualize. Will be coerced to a
matrix if necessary.}

\item{row_labels}{Optional character vector of labels for rows. If \code{NULL}
(default), uses \code{rownames(mat)}.}

\item{col_labels}{Optional character vector of labels for columns. If
\code{NULL} (default), uses \code{colnames(mat)}.}

\item{use_gg}{Logical; if \code{TRUE}, creates a \pkg{ggplot2} plot instead
of using base graphics. Requires the \pkg{ggplot2} package. Default is
\code{FALSE}.}

\item{scale}{Logical; if \code{TRUE}, standardizes values (subtracts mean,
divides by SD) before plotting. Default is \code{FALSE}.}

\item{log}{Logical; if \code{TRUE}, applies natural log transformation to
values before plotting. Default is \code{FALSE}.}

\item{includeMarginals}{Logical; if \code{TRUE} and \code{use_gg = TRUE},
adds marginal histograms using \pkg{ggExtra}. For base graphics, adds
marginal rug plots. Default is \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link{heatMap}} when
\code{use_gg = FALSE}.}
}
\value{
When \code{use_gg = TRUE}, invisibly returns the ggplot object.
  Otherwise, invisibly returns \code{NULL}. Called for its side effect of
  producing a plot.
}
\description{
A convenience wrapper around \code{\link{heatMap}} for fast visualization of
matrix-like objects. Can use either base R graphics or \pkg{ggplot2} for a
more polished appearance.
}
\details{
When \code{use_gg = TRUE}, the function creates a tile plot using
\code{\link[ggplot2]{geom_tile}} with a white-to-steelblue gradient. Marginal
histograms can be added using \pkg{ggExtra} if available.
}
\examples{
# Create a correlation matrix
cor_mat <- cor(mtcars[, 1:5])

# Quick base R plot
heatmap2(cor_mat, row_labels = colnames(cor_mat),
         col_labels = colnames(cor_mat))

\dontrun{
# ggplot2 version
heatmap2(cor_mat, use_gg = TRUE)
}

}
\seealso{
\code{\link{heatMap}} for the underlying scatter-to-grid
  interpolation, \code{\link{image2}} for simple matrix plotting
}
